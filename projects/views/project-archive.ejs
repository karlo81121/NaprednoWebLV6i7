<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" />
</head>

<body class="antialiased">
    <div class="relative d-flex flex-column sm:items-center min-h-screen bg-dots-darker bg-center bg-gray-100 selection:bg-red-500 selection:text-white">
        <div class="d-inline">
        <h1 class="mt-3 font-semibold">Archived projects</h1>
        </div>

        <h1>My opened projects</h1>


    <form action="/projects/archive" method="GET" class="w-50"></form>
    <div class="container mt-5 w-100">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                    <th scope="col">Price</th>
                    <th scope="col">Jobs done</th>
                    <th scope="col">Start date</th>
                    <th scope="col">End date</th>
                    <th scope="col">Members</th>
                </tr>
            </thead>
            <tbody>
                <% for(var i = 0; i < projects.length; i++) {%>  
                <tr>
                    <td><%= projects[i].name %></td>
                    <td><%= projects[i].description %></td>
                    <td><%= projects[i].price %></td>
                    <td><%= projects[i].jobs %></td>
                    <td><%= projects[i].start %></td>
                    <td><%= projects[i].end %></td>
                    <td><%= projects[i].members %></td>
            </tbody>
        </table>
    </div>

    <h1>Member:</h1>

    <form action="/projects/" method="GET" class="w-50"></form>
    <div class="container mt-5 w-100">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                    <th scope="col">Price</th>
                    <th scope="col">Jobs done</th>
                    <th scope="col">Start date</th>
                    <th scope="col">End date</th>
                    <th scope="col">Members</th>
                    <th scope="col">Manager</th>
                </tr>
            </thead>
            <tbody>
                <% for(var i = 0; i < projects.length; i++) {%>  
                <tr>
                    <td><%= projects[i].name %></td>
                    <td><%= projects[i].description %></td>
                    <td><%= projects[i].price %></td>
                    <td><%= projects[i].jobs %></td>
                    <td><%= projects[i].start %></td>
                    <td><%= projects[i].end %></td>
                    <td><%= projects[i].members %></td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

</body>
</html>