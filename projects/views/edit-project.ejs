<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <title>Edit project</title>
</head>
<body>

    <h1 class="container mt-5">Edit Project</h1>

    <div class="container">
        <form action="/projects/edit/<%= project._id %>/update" method="PUT" class="w-50 mt-5">
            <input type="hidden" name="_id" value="<%= project._id %>">
            <div class="form-group">
                <input type="text" class="form-control mt-2" value="<%= project.name %>" name="name" placeholder="Name">
            </div>
            <div class="form-group">
                <input type="text" class="form-control mt-2" value="<%= project.description %>" name="description" placeholder="Description">
            </div>
            <div class="form-group">
                <input type="number" class="form-control mt-2" value="<%= project.price %>" name="price" placeholder="Price">
            </div>
            <div class="form-group">
                <input type="text" class="form-control mt-2" value="<%= project.jobs %>" name="jobs" placeholder="Jobs done">
            </div>
            <div class="form-group">
                <input type="date" class="form-control mt-2" value="<%= project.start %>" name="start" placeholder="Start date">
            </div>
            <div class="form-group">
                <input type="date" class="form-control mt-2" value="<%= project.end %>" name="end" placeholder="End date">
            </div>
            <div class="form-group">
                <input type="text" class="form-control mt-2" value="<%= project.members %>" name="members" placeholder="Members">
            </div>
            <button type="submit" class="btn btn-success mt-2 w-100 justify-center"><a class="text-white text-decoration-none" href="/projects/edit/<%= project._id %>/update">Edit</a></button>
          </form>
    </div>

</body>
</html>