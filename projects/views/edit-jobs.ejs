<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" />
</head>

<body class="antialiased">
    <div class="relative d-flex flex-column sm:items-center min-h-screen bg-dots-darker bg-center bg-gray-100 selection:bg-red-500 selection:text-white">
    <h1 class="container mt-5 font-semibold">Edit jobs</h1>

    <div class="container">
        <form action="/projects/<%= project._id %>/edit" method="POST" class="w-50 mt-5">
            <div class="form-group">
                <input type="text" class="form-control mt-2" value="<%= project.jobs %>" name="jobs" placeholder="Jobs done">
            </div>
            <button type="submit" class="btn bg-dark text-white mt-2 w-100 justify-center"><a class="text-white text-decoration-none" href="/projects/<%= userID %>/edit/<%= project._id %>/update_jobs">Edit</a></button>
        </form>
    </div>

</body>
</html>